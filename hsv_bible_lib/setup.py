from setuptools import setup

setup(
    name='hsv_bible_lib',
    version='0.1.0',
    description='Library to look up bible texts from the HSV bible',
    packages=['hsv_bible_lib'],
    test_suite="tests",
    install_requires=[],
)
